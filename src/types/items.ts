export interface IItem {
    name: string;
    brand: string;
    price: number;
    desc: string;
    quantity: number;
    category: number[];
    imageUrl: string[];
}

export interface ExIItems {
    id: string;
}
